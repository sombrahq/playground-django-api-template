
### Authentication
# Simple token authentication
# username and password are the same as the once created for the admin

POST {{base_url}}/api/token/
Content-Type: application/json

{
  "username": "{{username}}",
  "password": "{{password}}"
}

> {%
    client.global.set("token", response.body.token);
%}
