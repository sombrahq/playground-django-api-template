apiVersion: batch/v1
kind: CronJob
metadata:
  name: django-api-template-cron-clearsessions
spec:
  # every hour
  schedule: "0 * * * *"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: django-api-template-cron-clearsessions
              image: django-api-template
              command:
                - python
                - manage.py
                - clearsessions
              envFrom:
                - configMapRef:
                    name: django-api-template-config
                - secretRef:
                    name: django-api-template-secrets
          restartPolicy: Never
      backoffLimit: 4
